<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.smyy.sharetour.buyer">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 获取运营商信息，用于支持提供运营商信息相关的接口 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/XqcTheme.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".my.view.SettingsActivity" />
        <activity android:name=".my.view.AccountSettingsActivity" />
        <activity android:name=".my.view.EditNicknameActivity" />
        <activity android:name=".my.view.EditUserIntroActivity" />
        <activity android:name=".my.view.MessageSettingsActivity" />
        <activity android:name=".ui.Test1Activity" />
        <activity android:name=".ui.test2.Test2Activity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".SelectAreaCodeActivity" />
        <activity android:name=".SetPwdActivity" />
        <activity android:name=".GuideLoginActivity" />
        <activity android:name=".PwdLoginActivity"></activity>
    </application>

</manifest>